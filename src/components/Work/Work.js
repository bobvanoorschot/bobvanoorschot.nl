import React from 'react';
import styled from "styled-components"

const Container = styled.section`
  height: 100vh;
  background-color: var(--dred);
`

export default function Work() {
  return <Container>Recent Work</Container>
}